name: Build Release

on:
  push:
    tags:
      - "*"
  workflow_dispatch:
    inputs:
      tag:
        description: Version to build, for example 1.6.10
        required: true
        type: string
      publish:
        description: Create GitHub Release and deploy to website
        required: true
        default: false
        type: boolean

permissions:
  contents: write

env:
  SOLUTION_FILE: GI-Subtitles.sln
  BUILD_CONFIGURATION: Release
  BUILD_PLATFORM: x64
  RELEASE_TAG: ${{ github.event_name == 'workflow_dispatch' && inputs.tag || github.ref_name }}

jobs:
  release:
    runs-on: windows-2022
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          submodules: recursive

      - uses: microsoft/setup-msbuild@v2
      - uses: NuGet/setup-nuget@v2
        with:
          nuget-version: 6.x

      - name: Validate release version
        shell: pwsh
        run: |
          $tagVersion = $env:RELEASE_TAG.TrimStart('v')
          $numericTagVersion = ($tagVersion -split '-', 2)[0]
          $assemblyInfo = Get-Content "GI-Subtitles/Properties/AssemblyInfo.cs" -Raw
          if ($assemblyInfo -notmatch 'AssemblyVersion\("([0-9.]+)"\)') {
            throw "AssemblyVersion was not found."
          }
          $assemblyVersion = [Version]$Matches[1]
          $requestedVersion = [Version]$numericTagVersion
          if ($assemblyVersion.ToString(3) -ne $requestedVersion.ToString(3)) {
            throw "Tag version $tagVersion does not match AssemblyVersion $assemblyVersion."
          }

      - name: Restore packages
        run: |
          nuget restore ${{ env.SOLUTION_FILE }}
          dotnet restore "WixSharp Setup/WixSharp Setup.csproj"

      - name: Cache OCR model bundle
        uses: actions/cache@v4
        with:
          path: .model-cache
          key: ocr-models-${{ hashFiles('inference/models.json') }}

      - name: Restore verified OCR models
        shell: pwsh
        run: .\scripts\Restore-OcrModels.ps1 -CacheDirectory ".model-cache"

      - name: Build application and tests
        run: |
          msbuild ${{ env.SOLUTION_FILE }} -t:GI-Subtitles:Rebuild -p:Configuration=${{ env.BUILD_CONFIGURATION }} -p:Platform=${{ env.BUILD_PLATFORM }}
          msbuild ${{ env.SOLUTION_FILE }} -t:GI-Test:Rebuild -p:Configuration=${{ env.BUILD_CONFIGURATION }} -p:Platform=${{ env.BUILD_PLATFORM }}

      - name: Run unit tests
        run: dotnet vstest GI-Test/bin/Release/GI-Test.dll /Tests:MissingTargetConfig_CreatesFreshConfigInsteadOfReusingPreviousGame,NewManifestVersion_IsOfferedOnlyWhenNotIgnored,ZeroRollout_DoesNotOfferUpdate /logger:trx /ResultsDirectory:test-results

      - name: Run OCR model smoke tests
        shell: pwsh
        run: |
          dotnet vstest GI-Test/bin/Release/GI-Test.dll `
            /Tests:TestSettingsWindowLoadsV6ByDefault,TestLegacyChineseModelsAreNotAvailableAtRuntime,TestRuntimePackageContainsOnlyRequiredOcrModels,TestV6UsesDedicatedV4RecognizerForJapanese,TestDetectionSettingsAreLoadedFromEachModelYaml `
            /logger:trx `
            /ResultsDirectory:test-results

      - name: Assemble release directory
        shell: pwsh
        run: |
          $releaseDir = "release-package"
          $buildDir = "GI-Subtitles/bin/Release"
          New-Item -ItemType Directory -Force -Path $releaseDir | Out-Null
          Copy-Item -Path "$buildDir/*" -Destination $releaseDir -Recurse -Force

          $required = @(
            "GI-Subtitles.exe",
            "Config.json",
            "inference/Det/V6/PP-OCRv6_tiny_det_infer/slim.onnx",
            "inference/Rec/V6/PP-OCRv6_tiny_rec_infer/slim.onnx",
            "inference/Rec/V4/jp_PP-OCRv4_mobile_rec_infer/slim.onnx",
            "inference/Rec/V4/jp_PP-OCRv4_mobile_rec_infer/dict.txt",
            "x64",
            "onnxruntime.dll",
            "onnxruntime_providers_openvino.dll",
            "dll/x64/OpenCvSharpExtern.dll",
            "dll/x64/opencv_videoio_ffmpeg4110_64.dll"
          )
          foreach ($item in $required) {
            if (-not (Test-Path (Join-Path $releaseDir $item))) {
              throw "Required release item is missing: $item"
            }
          }

          $unexpectedModels = Get-ChildItem (Join-Path $releaseDir "inference") -Recurse -File |
            Where-Object {
              $_.Extension -eq ".onnx" -and
              $_.FullName -notmatch "PP-OCRv6_tiny_(det|rec)_infer" -and
              $_.FullName -notmatch "jp_PP-OCRv4_mobile_rec_infer"
            }
          if ($unexpectedModels) {
            throw "Unexpected OCR models were included in the release: $($unexpectedModels.FullName -join ', ')"
          }

      - name: Install WiX Toolset CLI
        shell: pwsh
        run: |
          $wixToolDirectory = Join-Path $env:RUNNER_TEMP "wix-tool"
          dotnet tool install wix --version 6.0.1 --tool-path $wixToolDirectory
          if ($LASTEXITCODE -ne 0) {
            throw "Failed to install WiX Toolset CLI."
          }

          $wixToolDirectory | Out-File -FilePath $env:GITHUB_PATH -Encoding utf8 -Append
          & (Join-Path $wixToolDirectory "wix.exe") --version
          if ($LASTEXITCODE -ne 0) {
            throw "WiX Toolset CLI was installed but could not be executed."
          }

          # WixSharp otherwise resolves the latest UI extension on a clean
          # runner. Pin it to WiX 6 to avoid accidentally pulling WiX 7.
          & (Join-Path $wixToolDirectory "wix.exe") extension add -g WixToolset.UI.wixext/6.0.1
          if ($LASTEXITCODE -ne 0) {
            throw "Failed to install WixToolset.UI.wixext 6.0.1."
          }

          $installedExtensions = & (Join-Path $wixToolDirectory "wix.exe") extension list -g
          if ($installedExtensions -notcontains "WixToolset.UI.wixext 6.0.1") {
            throw "WixToolset.UI.wixext 6.0.1 was not found in the WiX extension cache."
          }

      - name: Build MSI
        shell: pwsh
        run: |
          $installerFiles = "WixSharp Setup/bin/Files"
          New-Item -ItemType Directory -Force -Path $installerFiles | Out-Null
          Copy-Item -Path "release-package/*" -Destination $installerFiles -Recurse -Force
          dotnet build "WixSharp Setup/WixSharp Setup.csproj" --configuration Release --no-restore
          if ($LASTEXITCODE -ne 0) {
            throw "MSI project build failed."
          }

          $msi = Get-Item "WixSharp Setup/GI-Subtitles.msi"
          if ($msi.Length -lt 1MB) {
            throw "Generated MSI is unexpectedly small: $($msi.Length) bytes"
          }
          Copy-Item $msi.FullName "GI-Subtitles-${env:RELEASE_TAG}.msi" -Force

      - name: Create ZIP and checksums
        shell: pwsh
        run: |
          Compress-Archive -Path "release-package/*" -DestinationPath "GI-Subtitles-${env:RELEASE_TAG}.zip" -Force
          Get-FileHash "GI-Subtitles-${env:RELEASE_TAG}.zip", "GI-Subtitles-${env:RELEASE_TAG}.msi" -Algorithm SHA256 |
            ForEach-Object { "$($_.Hash.ToLower())  $([IO.Path]::GetFileName($_.Path))" } |
            Set-Content "GI-Subtitles-${env:RELEASE_TAG}-SHA256.txt"

      - name: Generate release notes
        shell: pwsh
        env:
          GH_TOKEN: ${{ github.token }}
          MANUAL_TEST: ${{ github.event_name == 'workflow_dispatch' && inputs.publish == false }}
        run: |
          $notes = ""

          # An annotated tag keeps the message entered with `git tag -a`.
          $tagType = git cat-file -t "refs/tags/${env:RELEASE_TAG}" 2>$null
          if ($LASTEXITCODE -eq 0 -and $tagType -eq "tag") {
            $notes = (git for-each-ref "refs/tags/${env:RELEASE_TAG}" --format="%(contents)") -join "`n"
            $notes = $notes.Trim()
          }

          # When a tag/release is created on github.com, its description is the
          # Release body rather than an annotated Git tag message.
          if ([string]::IsNullOrWhiteSpace($notes) -and $env:MANUAL_TEST -ne "true") {
            # The tag push event can be queued just before the Release API has
            # finished publishing the new Release, so retry briefly.
            for ($attempt = 1; $attempt -le 5; $attempt++) {
              $existingNotes = gh api "repos/${{ github.repository }}/releases/tags/${env:RELEASE_TAG}" --jq .body 2>$null
              if ($LASTEXITCODE -eq 0) {
                $notes = ($existingNotes -join "`n").Trim()
                if (-not [string]::IsNullOrWhiteSpace($notes)) {
                  break
                }
              }
              if ($attempt -lt 5) {
                Start-Sleep -Seconds 3
              }
            }
          }

          if ([string]::IsNullOrWhiteSpace($notes) -and $env:MANUAL_TEST -eq "true") {
            $notes = "Manual test build for version ${env:RELEASE_TAG}."
          }

          # Lightweight tags without a description still get useful notes.
          if ([string]::IsNullOrWhiteSpace($notes)) {
            $generatedNotes = gh api --method POST "repos/${{ github.repository }}/releases/generate-notes" `
              -f "tag_name=${env:RELEASE_TAG}" `
              -f "target_commitish=${{ github.sha }}" `
              --jq .body
            if ($LASTEXITCODE -ne 0) {
              throw "Failed to generate release notes."
            }
            $notes = ($generatedNotes -join "`n").Trim()
          }

          Set-Content "github-release-notes.md" -Value $notes -Encoding utf8

      - name: Create website release manifest
        shell: pwsh
        run: |
          $version = $env:RELEASE_TAG.TrimStart('v')
          $msiName = "GI-Subtitles-${env:RELEASE_TAG}.msi"
          $msi = Get-Item $msiName
          $manifest = [ordered]@{
            schemaVersion = 1
            version = $version
            publishedAt = (Get-Date).ToUniversalTime().ToString("o")
            channel = "stable"
            rollout = 0
            minimumVersion = $null
            releaseNotes = (Get-Content "github-release-notes.md" -Raw).Trim()
            assets = [ordered]@{
              "windows-x64-msi" = [ordered]@{
                url = "https://2langs.com/data/$msiName"
                sha256 = (Get-FileHash $msiName -Algorithm SHA256).Hash.ToLowerInvariant()
                size = $msi.Length
              }
            }
          }
          $manifest | ConvertTo-Json -Depth 5 | Set-Content "release-manifest.json" -Encoding utf8

      - name: Upload workflow artifacts
        uses: actions/upload-artifact@v4
        with:
          name: GI-Subtitles-${{ env.RELEASE_TAG }}
          path: |
            GI-Subtitles-${{ env.RELEASE_TAG }}.zip
            GI-Subtitles-${{ env.RELEASE_TAG }}.msi
            GI-Subtitles-${{ env.RELEASE_TAG }}-SHA256.txt
            release-manifest.json
          retention-days: 30

      - name: Create GitHub Release
        if: ${{ github.event_name == 'push' || inputs.publish }}
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ env.RELEASE_TAG }}
          target_commitish: ${{ github.sha }}
          prerelease: ${{ contains(env.RELEASE_TAG, '-') }}
          body_path: github-release-notes.md
          fail_on_unmatched_files: true
          files: |
            GI-Subtitles-${{ env.RELEASE_TAG }}.zip
            GI-Subtitles-${{ env.RELEASE_TAG }}.msi
            GI-Subtitles-${{ env.RELEASE_TAG }}-SHA256.txt

  deploy-website:
    name: Deploy website downloads
    # Upstream only: forks lack the deploy secrets and must never touch the
    # stable channel, even if a bare (un-hyphenated) tag is pushed by accident.
    if: ${{ github.repository == 'qew21/Genshin-Subtitles' && (github.event_name == 'push' || inputs.publish) && !contains(github.event_name == 'workflow_dispatch' && inputs.tag || github.ref_name, '-') }}
    needs: release
    runs-on: ubuntu-24.04
    environment: production
    steps:
      - name: Download release artifacts
        uses: actions/download-artifact@v4
        with:
          name: GI-Subtitles-${{ github.event_name == 'workflow_dispatch' && inputs.tag || github.ref_name }}
          path: release-artifacts

      - name: Upload installer and checksum
        uses: appleboy/scp-action@v1
        with:
          host: ${{ secrets.DEPLOY_HOST }}
          port: ${{ secrets.DEPLOY_PORT || 22 }}
          username: release-deploy
          password: ${{ secrets.DEPLOY_PASSWORD }}
          source: release-artifacts/GI-Subtitles-${{ github.event_name == 'workflow_dispatch' && inputs.tag || github.ref_name }}.msi,release-artifacts/GI-Subtitles-${{ github.event_name == 'workflow_dispatch' && inputs.tag || github.ref_name }}-SHA256.txt
          target: /srv/2langs/data
          strip_components: 1
          overwrite: true

      # Publish the manifest last so clients never see a release before its MSI exists.
      - name: Publish release manifest
        uses: appleboy/scp-action@v1
        with:
          host: ${{ secrets.DEPLOY_HOST }}
          port: ${{ secrets.DEPLOY_PORT || 22 }}
          username: release-deploy
          password: ${{ secrets.DEPLOY_PASSWORD }}
          source: release-artifacts/release-manifest.json
          target: /srv/2langs/data
          strip_components: 1
          overwrite: true
